<% @page_title = "商品一覧 編集" %>
<h1><%= @page_title %></h1>

div class="toolbar"><%= link_to "商品の詳細に戻る", [:admin,@item] %></div>

<%= form_for [:admin, @item] do |form| %>
  <%= render "shared/item_form", form: form %>
  <div><%= form.submit "更新"%></div>
<% end %>
